aiogram==3.31.0
instaloader==4.15.3
pymongo>=4.8,<5
python-dotenv==1.1.1

aiohttp>=3.9,<4

# Public profile URL fetching (instagram.py). requests is also pulled in by
# instaloader; beautifulsoup4 is optional at runtime (the code falls back to
# plain HTML/JSON parsing when it is missing).
requests>=2.28,<3
beautifulsoup4>=4.12,<5
